class Playhead {
  // fields
  Boolean playing;
  
  // constructor
  Playhead() {
    playing = false;
  }
  
  
  // methods
  void drawPlayhead(float x, float y, float h) {  // draw playhead
    // main line
    
    line(x, y, x, h);
    
    //top section
    
  }
}
